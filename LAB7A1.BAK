#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<stdlib.h>


struct employee
{
	char id[10];
	char salary[10];
}obj;
void main(void)
{
	int numStudents;
    int x;
	clrscr();
	printf("Enter the num of employees : \n");
	scanf("%d",&numStudents);
    STUDENT* students = malloc(numStudents * sizeof *students);
   
    for (x = 0; x < numStudents; x++)
    {
        students[x].id=(char*)malloc(sizeof(char*));
       
        printf("Enter ID :");
        scanf("%s",obj[x].id);
		
        students[x].salary=(char*)malloc(sizeof(char*));
        printf("Enter salary :");
        scanf("%s",obj[x].salary);


    }

    for (x = 0; x < numStudents; x++)
        printf("First Name: %s, salary: %s\n",obj[x].id,obj[x].salary);

    return (0);
}

	
	
}